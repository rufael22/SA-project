custom-kafka1:
  bootStrapServer: localhost:9092

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://10.200.2.32:8080/eureka/

spring:
  cloud:
    config:
      import-check.enabled: false

  application:
    name: Kafka-Server
